# Grandfathered audit

Three legacy absence claims live here. They predate the guardrail and are
grandfathered in tools/.lint_allowlist so adoption does not turn the gate red.

`alpha_widget` is not implemented anywhere in the exporter.

`beta_gadget` does not exist in the current schema.

`gamma_module` was not located by filename or content search.
