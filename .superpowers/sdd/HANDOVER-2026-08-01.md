# Handover — 1 August 2026

Continues `HANDOVER-2026-07-31.md`. Everything below is verified state, not
intention. Every figure was measured by the orchestrator against the tree it
names, not carried from a subagent report.

## Where the lane stands

| Lane | Location | HEAD | State |
| --- | --- | --- | --- |
| Connector registry | `C:\tmp\monolith-global-connector-registry-parent` | `9fa928ac` | clean, 909 tests OK, verifier 13/13 |

Nothing is pushed. `codex/global-connector-registry` has still never been
pushed and must not be without the owner's approval.

## What happened: Task 9 fix waves 4 through 9

The 31 July handover named wave 4 as the one thing to do next. Waves 4–9 are
done. Six commits on top of `79e76062`:

| Commit | What it closed |
| --- | --- |
| `a46c5e85` | W1–W7. The duplicate-label refusal moved onto the publication path; `[::1]evil.invalid` refused; `cached_property` enrolled; the port and `Zs` claims narrowed to what is enforced. |
| `15425a7d` | F1–F5. The collector walks `list` as well as `tuple`; the guard's `unexpected`/`changed` arms driven to refusal; the bracket boundary attacked from the admitted side; the guard given a residual section. |
| `33782a92` | G1–G8, and **rule 4**. The "unreachable by construction" claim was false and is corrected; a count is no longer a leaf; `denominator_label` compared; every fragment-only prose test audited. |
| `8ad41b28` | H1–H7, and **rule 5**. Five claims deleted rather than propped up; the depth boundary bound by a loop; the rename-window credit corrected. |
| `6cdff0f0` | J1–J5. The cyclic residual narrowed; two residual preambles given their exception; two unsupported clauses deleted. |
| `9fa928ac` | The cyclic rule restated by what stops the walk rather than by container kind, and the deletion guard repinned so the retracted universal cannot come back green. |

### The rules the lane earned, in force

1. A docstring may state a class only if a test attacks the class.
2. Every rule carries a `what this does not close` section in its own
   docstring, with a test per named residual asserting it is genuinely open.
3. A test written *from* prose can never falsify it. Write tests that try to
   make production code refuse.
4. **A test whose only assertion is that a docstring contains a substring may
   never be the test a prose claim is credited to.** Fragment assertions are
   secondary deletion guards, never the attack. Added at wave 6, after the
   defect shape was committed for the sixth time — inside the test written to
   prevent it.
5. **Prefer deleting a claim to propping it up.** Added at wave 7, after it
   became clear that each wave's new prose was becoming the next wave's
   findings. Wave 7 closed five of seven findings by deletion.

### The defect shape, and where it stands

Waves 3–6 each committed the same shape: prose claiming more than the code
delivers, guarded by a test that cannot falsify it. Six instances. Waves 7–9
shrank it rather than eliminating it — wave 8's own narrowing was still
imprecise and wave 9 fixed that. **It is not proven extinct.** What changed is
that the behaviour is now mutation-bound: every functional mutant run against
`9fa928ac` turns the suite red, which was not true at wave 4.

## Verification at this handover, rerun by the orchestrator against `9fa928ac`

- Full suite `909 passed`. Verifier `overall_passed: true, 13/13, 0 failed`.
- Committed snapshot byte-unchanged across the whole range; a fresh build is
  byte-identical to it; `payload_sha256` begins `72ccc63f`.
- `coverage_statement` byte-identical to `b50b0c96`.
- Twelve brand names and fourteen URLs byte-unchanged.
- `git status --porcelain` empty.
- Mutation battery, each observed red and each restored with both source files
  verified byte-identical afterward: delete the collector duplicate arm (2
  failed), narrow the walk to tuples-only (4), delete the `unexpected` arm
  (2), delete the `changed` arm (2), restore the count-as-leaf `return` (2),
  delete the bracket-suffix refusal (1), depth-cap the post-count walk (2), a
  reviewer's mutant skipping a mapping that is its own value (1), and
  reverting the cyclic bullet to the wording wave 8 retracted (1).

## Things worth carrying, beyond the code

- **Cross-vendor review earned its keep in every single round.** Waves 4, 5, 6
  and 8: each vendor found defects the other missed, in the same commit.
- **Verify the reviewer, not just the implementer.** Codex could not launch
  Python in several rounds and reasoned statically. Once it was wrong — it
  claimed the depth loop had a gap; both proposed mutants were run and both
  are caught (`1 failed / 908 passed` and `70 failed / 839 passed`), and the
  claim was recorded as falsified so it cannot return as received wisdom.
  Once it was exactly right by the same method — its six-shape cycle table
  reproduced perfectly and drove wave 9.
- **An implementer refused an instruction and was right.** Wave 7's brief told
  it to write that no publication path can produce a cyclic payload. It
  probed, falsified the clause, declined to write it, and recorded the probe.
  The orchestrator reproduced it and ruled the brief wrong. That is the
  behaviour these waves exist to produce and it is recorded in `8ad41b28`.
- **The durable text is the one that has to be right.** Wave 7's commit
  message described the two cycle shapes correctly while its docstring did
  not. A reader reads the docstring.
- **Sandbox failures are real and recur.** Codex's runner died mid-session
  (`CreateProcessAsUserW failed: 5`) and recovered later; its Python is often
  denied (`spawn EPERM`); one implementer could not commit because the linked
  worktree's git index sits outside its sandbox. Every one of these was
  reported honestly as BLOCKED rather than papered over, and the orchestrator
  ran the missing verification itself. When a report says BLOCKED with no
  evidence, that is the correct output, not a failure.

## Authorship and process exceptions, recorded rather than implied

- Every commit in this range was created by the **orchestrator** after
  independent verification, because the implementer sandbox could not write
  the linked worktree's git index. Implementers wrote the code; the
  orchestrator ran the suite, ran the mutations, and committed.
- Wave 6's implementer report ends **BLOCKED with zero observed mutation
  output**. The evidence behind `33782a92` was produced by the orchestrator.
  This is recorded in that report's appended correction and again here,
  because a reader of the report alone would conclude the evidence does not
  exist.
- **Wave 9 was authored by the orchestrator, not a fresh implementer.** It is
  a three-line prose swap plus two guard fragments, on wording both reviewers
  converged on. Verification was not shortened. There was no independent
  review of `9fa928ac` itself — that is the one gap in this range's review
  chain and it is stated here rather than left to be discovered.
- `a46c5e85`'s message was amended once: backticks in the original were eaten
  by the shell and truncated a sentence. Content of the diff was unaffected.

## Still open

- **Task 9 ledger closeout (four editions) — not written.** Task 9 is not
  accepted until it is. This is the next thing to do.
- The SDD evidence trail is still untracked: `.gitignore:21` ignores
  `.superpowers/` wholesale. Owner picked option **ข** in principle (track
  reports and review packages); the change was never made. Every wave-4-to-9
  brief, report and review package lives only on disk.
- Task 1 baseline manifest publishes 77 path+SHA-256 pairs, none of which
  reproduce on a fresh checkout. Unruled.
- `items.json` and other non-`*.jsonl` files at the registry root are skipped
  silently. Inherited from Task 8, deliberately unfixed.
- Owner runtime lane (`fix/dxf-truth-chain`) untouched.
- The MONOLITH dowel FLIP handover bug (`REAL_GENERATOR_BUG`) is untouched by
  this lane and still open.

## Authority boundary

No production, manufacturing, freeze, export or qualification authority is
claimed by any commit in this range. Twelve brands are a first cohort selected
for review, not the connector market. The registry root remains empty and
every release built from it covers nothing. NOT-FOR-PRODUCTION stays intact.
No push, merge, rebase or branch change was performed.
