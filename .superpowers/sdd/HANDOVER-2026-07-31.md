# Handover — 31 July 2026, written at 97% context

Written to disk deliberately: the session is about to run out. Everything below
is verified state, not intention.

## Where every lane stands

| Lane | Location | HEAD | State |
| --- | --- | --- | --- |
| Connector registry | `C:\tmp\monolith-global-connector-registry-parent` | `79e76062` | clean, 888 tests OK, verifier 13/13 |
| Product repo `main` | GitHub `indetailsgroup-hue/monolith-workspace` | `57b69513` | PR #33, #34, #35 all merged; zero red |
| CI truthfulness | `C:\tmp\ci-tidy` | `5bbee3f1` | merged via #35 |
| book-to-skill unified | `C:\tmp\book-to-skill-unified` | `2dae18f7` | merged into main via #33 |
| eslint fix | `C:\tmp\eslint-stale` | `c7f229c7` | merged via #34 |

Nothing is pushed from the registry lane. `codex/global-connector-registry` has
never been pushed and must not be without the owner's approval.

## THE ONE THING TO DO NEXT: Task 9 fix wave 4

Two independent reviews of `79e76062` both returned **NEEDS_FIXES**. They were
run against the same commit by two different vendors and **each found things the
other missed** — which is the whole argument for the cross-vendor rule.

Brief to write: `.superpowers/sdd/task-9-fix-wave-4-brief.md`. Findings below,
severity order, each already reproduced by its reviewer.

### W1 (Claude, strongest finding of either review) — the duplicate-label refusal is not on any publication path

`coverage.py:1997` claims two counts sharing a label are *"**refused** rather
than published."* Measured:

```
CoverageSnapshot.dup = property(lambda s: mk("first_cohort_brands_with_a_source_read"))
  snapshot.counts      -> ValueError
  build_release(...)   -> SUCCEEDS, payload_sha256 72ccc63f...
  snapshot_payload(...)-> SUCCEEDS, 8930 bytes
```

`grep -rn "\.counts" packages/ tools/` outside the two count mappings returns
**only docstrings and a comment**. No production path reads `counts`.
`snapshot_payload` builds from its own hand-written field list. So a record with
two counts under one label is published, released and digested with no refusal.

The test `test_no_two_counts_share_a_label` reads `counts` — it was written from
the sentence, so it sees the refusal it was told to expect, and never asks
whether **publication** is refused. Fourth instance of the lane's defect shape.

### W2 (both reviewers, independently) — `CountEnrollmentResidualTests` does not exercise each named residual

`coverage.py:2001` says *"Each is exercised by `CountEnrollmentResidualTests`."*

| Named residual | Tested? |
| --- | --- |
| property returning a `tuple` of counts | yes |
| **"or a mapping of mappings"** | **no** |
| plain class attribute | yes |
| **"or a dataclass field"** | **no** |
| "an enrolment check, not an arithmetic one" | **no** |

The behaviour claims are true — all four shapes are genuinely not enrolled. What
is false is the coverage claim, in the one place wave 3's brief made binding.
`STILL_OPEN_URL_CASES` (9/9) and `STILL_OPEN_BRAND_NAME_CASES` (4/4) both comply
exactly; `counts` is the one written to the pattern's shape without its table.

### W3 (Claude) — `functools.cached_property` is silently not enrolled and no residual names it

Walk is `isinstance(attribute, property)`. `CoverageSnapshot` is a frozen
dataclass without `slots`, so `cached_property` works on it and is the idiomatic
way to memoise a derived count. Measured: `cached_property enrolled: False`,
value reachable via `getattr`. Named residuals are "plain class attribute" and
"dataclass field"; a `cached_property` is neither.

### W4 (Codex, and the finding Claude did not reach) — `[::1]evil.invalid` is a real consumer divergence

The bracketed-host branch never validates what follows `]`.

```
this module:  ADMIT host="[::1]"          https://[::1]evil.invalid/x
.NET System.Uri: host [::1], port 443     https://[::1]evil.invalid/x
```

A reader sees `evil.invalid`; a fetcher goes to `::1`. That is precisely the
reviewer/fetcher disagreement used to justify refusing userinfo, so the same
argument applies. Codex recommends **refusal**, not documentation.

### W5 (both) — the port is never parsed, and no residual says so

```
[ADMIT] https://host:abc/x     [ADMIT] https://host:-1/x
[ADMIT] https://host:99999999999/x   [ADMIT] https://a:b:c/x   [ADMIT] https://]/x
```

RFC 3986 §3.2.3 is `port = *DIGIT`. Both residual bullets are host-scoped.
Neither function claims to check the port — but the section is titled *what this
rule does not close*.

### W6 (Codex) — "every other `Zs` is refused **by name**" is false for an all-`Zs` name

`_require_brand_name` calls `_require_string` first, which does an unrestricted
`text.strip()` and raises the generic `brand_name must not be blank`. So a name
of only U+3000 is refused, but not *by name*. The test attacks U+3000 only when
appended to visible text.

### W7 (Claude, minor) — "two independent transcriptions … compared" is unverifiable

Two structurally identical ten-tuples, same order, same commit, same author.
Independence of authorship leaves no trace in the tree. Disclosed as
pass-by-construction, which is right; the word "independent" is not.

## What BOTH reviewers confirmed correct — do not re-litigate

- **The 268-code-point Unicode transcription is complete and correct.** Codex
  derived `Default_Ignorable_Code_Point` for 16.0.0 two independent ways:
  `missing=0, extra=0, redundant=0, wrong-category=0`. The docstring's honesty
  about being a transcription and not a derivation is sufficient.
- H1 (invisibles refused, pure-invisible names refused, `'X'`/`'X '` collide,
  12 names byte-unchanged), H2 (`counts` genuinely derived, full MRO walk),
  H3's ten-case matrix and six hostless cases, all nine URL residuals and all
  four brand residuals still admitted.
- Pass-by-construction disclosure is **exact**: 16 of 39, no undisclosed test.
  Verified by materialising the base implementation and running HEAD's tests
  against it — `24 failed, 132 passed`, 23 new + 1 modified.
- Regressions all hold: payload digest `72ccc63f…`, `coverage_statement`
  byte-identical to `b50b0c96`, both data files byte-unchanged, source counts
  partition `0 + 0 + 14 == 14`, `_require_brand_source_agreement` dual-enforced.
- H4 adopted: report §11.4 records **8, not 7**.

## Still open, older, not wave 4's

- Task 9 ledger closeout (four editions) — **not written yet**; Task 9 is not
  accepted until wave 4 lands.
- The SDD evidence trail is untracked: 54 files / 2 MB under `.superpowers/sdd/`,
  including all 14 artifacts whose digests the ledger publishes. Cause is
  `.gitignore:21` `.superpowers/` — a whole-directory ignore written to protect
  one brainstorm token file that does not exist in this repo. Secret-scanned
  clean (AWS/GitHub/private-key/Slack/bearer/supabase all 0). Owner picked
  option **ข** in principle (track reports + review packages) but the change was
  never made.
- Task 1 baseline manifest publishes 77 path+SHA-256 pairs, none of which
  reproduce on a fresh checkout. Unruled.
- `items.json` and other non-`*.jsonl` files at the registry root are skipped
  silently. Inherited from Task 8, deliberately unfixed.
- Owner runtime lane (`fix/dxf-truth-chain`) untouched all session.

## Working rules earned today — carry these

1. **A docstring may state a class only if a test attacks the class.** If you can
   only test instances, write instance-scoped prose. Tests written *from* prose
   can never falsify it; three waves went green while carrying false claims.
2. Every rule gets a `what this does not close` section **in the docstring**,
   with a test asserting each named residual is genuinely still admitted.
3. **Cross-vendor review is not optional.** Today's two reviews of one commit
   found different defects. Claude found W1 and W3; Codex found W4 and W6.
4. Do not leave uncommitted edits in a repo while a reviewer is verifying it —
   it contaminated one review and made a "clean tree" report unreproducible.
5. Heavy work goes to Codex (owner's instruction, 31 July).

## Environment notes

- Permission guard: `C:\Users\thai3\.claude\hooks\permission_guard.py`,
  registered as a PreToolUse hook with `permissions.defaultMode: acceptEdits`.
  Three tiers: deny / ask / allow. Proven firing. Covers Bash and PowerShell
  only — **not MCP tools**.
- Codex MCP: `mcp__codex__codex`. Do **not** pass `model: gpt-5.2-codex` — a
  ChatGPT account rejects it. Omit `model`. Its sandbox denied running
  `python.exe`, so it reviews by reading and by PowerShell probes.
- Never use `C:\Users\thai3\AppData\Local\Temp\pytest-of-thai3` — PermissionError.
